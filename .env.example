# MongoDB Atlas Connection
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority&appName=<appname>

# Port du serveur
PORT=5000

# JWT Secret (MINIMUM 32 caractères - générez une clé forte)
JWT_SECRET=your-super-secure-jwt-secret-key-minimum-32-characters

# Environnement
NODE_ENV=development

# URLs pour CORS en production (optionnel)
FRONTEND_URL=https://your-frontend-domain.com
PRODUCTION_URL=https://your-backend-domain.com

# Instructions:
# 1. Copiez ce fichier vers .env
# 2. Remplacez <username> par votre nom d'utilisateur MongoDB Atlas
# 3. Remplacez <password> par votre mot de passe MongoDB Atlas  
# 4. Remplacez <cluster> par le nom de votre cluster
# 5. Remplacez <database> par le nom de votre base de données
# 6. Remplacez <appname> par le nom de votre application
# 7. Générez un JWT_SECRET fort (32+ caractères avec lettres, chiffres, symboles)
# 8. En production, changez NODE_ENV=production